module secret is
    type SecretHandle : Linear;

    function AccessSecret(id: Nat64): SecretHandle;
    
    generic [R: Region]
    function RevealSecret(handle: &[SecretHandle, R]): Nat64;
    
    function WipeSecret(handle: SecretHandle): Unit;
end module.
